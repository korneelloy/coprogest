import { StateLabelPipe } from './state-label-pipe';

describe('StateLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new StateLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
