import { VoteLabelPipe } from './vote-label-pipe';

describe('VoteLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new VoteLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
