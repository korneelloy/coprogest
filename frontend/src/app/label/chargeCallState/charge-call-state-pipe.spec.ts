import { ChargeCallStatePipe } from './charge-call-state-pipe';

describe('ChargeCallStatePipe', () => {
  it('create an instance', () => {
    const pipe = new ChargeCallStatePipe();
    expect(pipe).toBeTruthy();
  });
});
